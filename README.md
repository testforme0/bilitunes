# BiliTune

一个用于研究浏览器扩展开发和页面媒体信息解析流程的 Chrome/Edge Manifest V3 示例项目。

当访问 B 站视频页时，点击浏览器工具栏里的插件图标，插件会从当前页面解析公开可见的播放信息，定位当前页面可用的音频流地址，并触发浏览器下载。若页面存在封面图，也会额外下载一张同名封面图片。

## 功能

- 仅支持 B 站视频页：`https://www.bilibili.com/video/*`
- 不包含登录状态处理
- 按当前页面可见播放信息中的优先级选择最佳可用音频流
- 下载音频时会同时尝试下载一张同名封面图
- 当前版本不做媒体重封装，不向音频文件内部写入封面或 metadata

## 项目结构

```text
bilibili-audio-extension/
  manifest.json
  background.js
  README.md
```

## 本地加载

1. 打开 Chrome 或 Edge 的扩展管理页面
2. 开启“开发者模式”
3. 选择“加载已解压的扩展程序”
4. 选中 `bilibili-audio-extension` 目录

## 使用方法

1. 打开任意 B 站视频页
2. 点击浏览器工具栏中的插件图标
3. 浏览器会下载当前页面解析到的音频文件
4. 如果页面存在封面图，浏览器还会额外下载一张 `-cover` 后缀的图片文件

## 技术说明

- 下载能力依赖当前页面中的 `window.__playinfo__`
- 如果页面结构或字段命名变化，插件需要同步调整
- 当页面内未直接暴露可用音频流时，插件会尝试使用 `bvid + cid` 请求 B 站播放接口作为兜底
- 当前实现保持轻量，未引入 `ffmpeg.wasm` 或其他媒体处理依赖

## 调试说明

- 插件会把最近一次执行结果保存到扩展本地存储 `chrome.storage.local.lastRun`
- 如果某个页面失败，可以在扩展的 service worker 控制台里查看 `lastRun` 对应的错误阶段和页面上下文

## 使用边界

- 本项目仅用于学习浏览器扩展开发、页面信息提取和下载流程处理
- 使用者应自行确认其使用行为符合目标网站规则、适用法律以及相关内容授权要求
- 请勿将本项目用于绕过登录、付费、会员或其他访问控制措施

## 免责声明

本项目不提供任何内容授权。仓库仅包含示例代码，不包含任何第三方音视频内容。因使用本项目产生的合规、版权或平台规则问题，由使用者自行承担责任。
